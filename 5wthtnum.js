// We are to return 5 without using any integer or any mathematical operator
// We declare the parameters of an array of five elements and we will get its length
// We return the integer 5
// unusualFive(color) => 5, unusualFive(tests) => 5, unusualFive(emacs) => 5
// We will input an array with five elements, find the length of the array and return the value

function unusualFive() {
    let arr = ['five', 'four', 'three', 'two', 'one']
    
     return arr.length
    
  }

  unusualFive(color)
  unusualFive(emacs)
  unusualFive(tests)
 unusualF
  // We also return string lengths, indexOf, 
 1 2 3 4 5 6 
 
